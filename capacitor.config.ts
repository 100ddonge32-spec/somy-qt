import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.somyqt.app',
  appName: 'Somy QT',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
